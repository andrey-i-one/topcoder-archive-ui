server {
    listen       80;
    server_name  "";

    location / {
        root   /usr/share/nginx/html;
        try_files $uri /index.html;
        index  index.html index.htm;
    }
    
    location /healthcheck {
        return 200 'OK';
    }
}
